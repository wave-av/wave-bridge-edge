node_modules
dist/
.wrangler/
.dev.vars
*.log

# Derived by scripts/ci/bundle-eval-gate.mjs from wrangler.toml on every gate run ([[containers]] +
# [[durable_objects.bindings]] removed so `wrangler dev` boots without invoking Docker — see the
# script's header for why that's a CI-budget decision, not a credential one).
# Generated, never authored: committing it would let it drift from the real Worker config.
wrangler.bundle-eval.generated.toml

# >>> wave claude-project-chassis (managed) — do not edit between these markers >>>
.claude/settings.local.json
.claude/*.local.json
.claude/env
.claude/state/
.claude/workflows/executions/
.claude/agent-memory/
.claude/.DS_Store
# <<< wave claude-project-chassis (managed) <<<
